@import '~Styles/mixins/breakpoints'

.toolbar
  height 60px
  position: fixed
  top: 0
  left: 0
  right: 0
  z-index: 100
  box-shadow: 0 1px 1px rgba(#000, .15)

  :global .navbar
    min-height: 60px

+media-breakpoint-up(md)
  .searchForm
    max-width: 400px

:global
  .next-toolbar
    padding-top: 60px
