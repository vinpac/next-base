$spacing-props = { 'margin': 'm' }
for $breakpoint in $grid-breakpoints
  +media-breakpoint-up($breakpoint)
    $infix = breakpoint-infix($breakpoint, $grid-breakpoints)

    for $prop, $abbrev in $spacing-props
      for $size, $length in $spacers
        .{$abbrev}{$infix}-{$size}
          {$prop}: $length !important

        .{$abbrev}t{$infix}-{$size}
          {$prop}-top:  $length !important

        .{$abbrev}r{$infix}-{$size}
          {$prop}-right:  $length !important

        .{$abbrev}b{$infix}-{$size}
          {$prop}-bottom: $length !important

        .{$abbrev}l{$infix}-{$size}
          {$prop}-left:  $length !important

        .{$abbrev}x{$infix}-{$size}
          {$prop}-right: $length !important
          {$prop}-left:  $length !important

        .{$abbrev}y{$infix}-{$size}
          {$prop}-top:    $length !important
          {$prop}-bottom: $length !important

    // Some special margin utils
    .m{$infix}-auto
      margin: auto !important

    .mt{$infix}-auto
      margin-top: auto !important

    .mr{$infix}-auto
      margin-right: auto !important

    .mb{$infix}-auto
      margin-bottom: auto !important

    .ml{$infix}-auto
      margin-left: auto !important

    .mx{$infix}-auto
      margin-right: auto !important
      margin-left: auto !important

    .my{$infix}-auto
      margin-top: auto !important
      margin-bottom: auto !important


.viewBlock
  padding: 1.25rem

.viewSpacer
  padding-top: 20px !important
  padding-bottom: 20px !important

.viewSpacerBottom
  padding-bottom: 3rem

.viewSpacer--large
  padding-top: 3rem
  padding-bottom: 3rem

.viewSpacer--x-1
  padding-top: 5rem
  padding-bottom: 5rem

.viewSpacer--x-2
  padding-top: 7rem
  padding-bottom: 7rem

.aspectRatio
  position: relative

.aspectRatioBody
  position: absolute
  top: 0
  left: 0
  right: 0
  bottom: 0
  margin: auto

.aspectRatioFill
  display: block
  width: 100%

.d--block
  display: block

.block--horizontal-large
  padding-left: 3rem !important
  padding-right: 3rem !important

.pad-0
  padding: 0 !important

.view-spacer
  padding-top: 5rem
  padding-bottom: 5rem

.section-spacer
  padding-top: 60px
  padding-bottom: 60px
